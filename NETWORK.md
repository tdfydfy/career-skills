# 房地产营销 Skill 网络

## 1. 目标

本仓库按“基础能力、业务技能、常用成果”三层组织房地产营销工作能力。

网络不是技能清单，而是一套可组合的工作系统：

```text
基础能力层：统一分类、标准、口径、知识和数据结构
      ↓
业务技能层：完成可复用的分析、判断、策划和执行工作
      ↓
常用成果层：围绕具体管理目的编排多项能力，形成固定交付物
      ↓
执行结果、复盘数据和新案例回流基础能力层
```

现有技能保持原目录、原名称和原内容不变，通过注册表接入网络。

## 2. 三层定义

### 2.1 基础能力层

基础能力层回答“依据什么标准工作”，包括：

- 项目分类、物业类型、营销阶段和经营健康度。
- 指标口径、财务标准、投资标尺和审批边界。
- 项目、客户、市场、货量、价格和费用的数据结构。
- 营销动作库以及动作适用条件。
- 文案、图片、视频、品牌和合规标准。
- 数据来源、更新时间、证据强度以及事实与假设的区分。
- 从公司经营意图、事实判断、核心矛盾到策略、动作、目标、预算和复盘的决策推导标准。

纯知识和标准优先建设为共享知识模块；需要接收输入并作出判断时，再建设为可执行技能。

### 2.2 业务技能层

业务技能层回答“怎样完成一项专业工作”，包括市场、客户、产品、价格、货量、策略、客储、媒体、渠道、活动、宣传、展示、销售、接待、签约回款、内容生产和组织沟通。

每个业务技能只解决一个边界清晰的问题，输出结构化中间结果，供多个成果技能重复使用。

### 2.3 常用成果层

常用成果层回答“为了某个经营目的，需要交付什么”。成果技能是编排技能，不是简单的写作模板。

每个成果技能必须定义：

1. 使用场景、目标读者和待解决的决策。
2. 必需输入、缺口数据和允许采用的假设。
3. 需要调用的基础能力与业务技能。
4. 调用顺序、判断分支和冲突处理原则。
5. 固定目录、输出模板和质量门槛。
6. 风险、备选方案、决策事项和后续动作。

## 3. 节点形态

网络中的节点分为三种形态：

| 节点形态 | 用途 | 典型例子 |
| --- | --- | --- |
| `reference-module` | 保存共享口径、知识和标准 | 财务标准、项目分类、图片标准 |
| `executable-skill` | 完成独立业务任务 | 客储分析、媒体策略、费用分析 |
| `orchestrator-skill` | 调用多个节点形成完整成果 | 首开报告、年度报告、项目诊断 |

不要把只有静态定义的内容拆成大量空技能，也不要把多个完整业务流程塞进一个万能技能。

## 4. 组合模型

一次营销工作采用以下组合：

```text
成果目的
+ 项目事实包
+ 基础标准
+ 业务分析与策略
+ 场景参数
+ 输出模板
+ 质量门槛
= 可审核、可决策、可执行的成果
```

统一场景参数：

```yaml
horizon: annual | half-year | quarter | month | campaign
property_type: residential | parking | commercial | mixed
sales_stage: pre-launch | launch | sustained | tail | delivery
operating_status: normal | key-difficult | tail | undetermined
primary_objective: sales | cashflow | profit | inventory | brand
organization_level: project | regional | headquarters
```

场景参数用于选择业务模块和参考资料，不应直接产生大量重复技能。

## 5. 公共数据契约

所有新增节点优先使用 `framework/contracts/` 下的公共契约：

- `project-context.yaml`：描述项目事实、分类、经营目标和数据边界。
- `market-context.yaml`：描述市场量价、结构、竞品、未来供应和情景。
- `customer-context.yaml`：描述客户画像、MAN条件、需求匹配和盘点阶段。
- `inventory-context.yaml`：描述房源、价格、库存角色和计划批次。
- `organization-context.yaml`：描述三级组织、经营目标、相关方和审批路径。
- `metric-snapshot.yaml`：描述指标值、统计范围、公式版本、比较周期和数据质量。
- `fact-record.yaml`：描述事实、假设、预测、证据等级和授权状态。
- `financial-context.yaml`：描述价格、利润、现金流、费用、权限和情景测算。
- `marketing-action-plan.yaml`：描述营销动作、责任、预算、指标和退出条件。
- `content-brief.yaml`：描述文案、图片、视频等内容任务及审核要求。
- `capability-result.yaml`：规范业务技能输出的事实、判断、建议和证据。
- `deliverable-request.yaml`：规范成果技能的目的、读者、依赖和质量门槛。
- `deliverable-package.yaml`：规范最终成果的结论、证据、策略、动作、财务和决策事项。

技能之间传递结构化结果，最终成果再翻译成报告、方案、说辞、软文或汇报材料。

任何关键结论都应能追溯到以下三类信息之一：

- 已验证事实。
- 明确标注的业务假设。
- 有来源、有日期的外部证据。

所有策略类成果还必须保留以下推导链：

```text
公司经营意图 → 项目/市场/客户事实 → 核心矛盾 → 策略命题与取舍
→ 价格/货量/价值选择 → 营销动作 → 目标漏斗与预算 → 验证和复盘
```

## 6. 目录与注册方式

```text
career-skills/
├── NETWORK.md
├── framework/
│   ├── skill-network.yaml
│   ├── task-router.yaml
│   ├── enterprise-inputs.yaml
│   ├── BUILD-ROADMAP.md
│   ├── contracts/
│   ├── foundations/
│   ├── business/
│   ├── deliverables/
│   ├── examples/
│   ├── knowledge/
│   ├── scripts/
│   └── templates/
├── <existing-skill>/
└── <future-skill>/
    ├── SKILL.md
    ├── agents/openai.yaml
    ├── references/
    ├── scripts/
    └── assets/
```

技能目录继续放在仓库根目录，确保可以独立发现和使用。层级、依赖和状态统一登记在 `framework/skill-network.yaml`，不通过移动目录表达。

新增节点时：

1. 先在注册表中填写节点职责、输入、输出和依赖。
2. 判断它应是共享知识模块、业务技能还是成果编排技能。
3. 确认没有与现有节点重复后，再初始化技能目录。
4. 使用公共契约定义输入输出。
5. 添加真实业务样例并验证。
6. 通过验收后，把状态从 `planned` 更新为 `active`。

## 7. 成果技能运行顺序

以首开报告为例：

```text
读取 deliverable request
  → 建立 project context
  → 校验项目阶段、指标、财务和投资边界
  → 调用市场、客户、产品等分析技能并提炼核心矛盾
  → 调用营销策略节点形成策略命题、备选路径和取舍
  → 调用价格、货量等专项技能
  → 调用客储、媒体、渠道、展示、活动、销售等策略技能
  → 形成首开策略主线、目标漏斗和预算映射
  → 检查目标、货量、客户、价格、动作、费用是否相互一致
  → 输出报告、风险、备选方案和待决策事项
```

成果技能负责最终一致性。业务技能之间结论冲突时，不得直接拼接，必须回到事实、口径和成果目标进行裁决。

## 8. 建设状态

注册表使用以下状态：

| 状态 | 含义 |
| --- | --- |
| `existing` | 仓库已有，尚未按新网络改造 |
| `planned` | 已登记，等待设计与开发 |
| `draft` | 已创建，正在补充内容 |
| `active` | 已验证，可投入使用 |
| `deprecated` | 停止新增使用，等待迁移 |

当前阶段已完成网络骨架、基础标准、业务工作流和成果编排草案，不改造现有技能。草案经企业校准和真实案例前测后，再逐个初始化为独立技能。

## 9. 当前覆盖与校准

- `framework/foundations/`：基础层共享标准草案。
- `framework/business/`：业务节点工作流规格。
- `framework/deliverables/`：常用成果编排配方。
- `framework/enterprise-inputs.yaml`：需要企业补充或确认的内容清单。
- `framework/task-router.yaml`：用户任务到现有技能、业务节点和成果节点的路由。
- `framework/BUILD-ROADMAP.md`：从草案到独立可用技能的实施顺序。
- `framework/examples/`：完全虚构的端到端契约样例，不作为企业或行业事实。

运行以下命令检查节点、依赖、路径、YAML、指标、动作和待补项：

```bash
python3 framework/scripts/validate_network.py
```

## 10. 单节点验收标准

每个新技能投入使用前至少满足：

- 职责单一，触发条件明确，与其他技能边界清楚。
- 输入不足时能指出缺口，不编造项目事实和数据。
- 输出符合公共契约，结论能追溯到事实或假设。
- 详细行业知识放入按需加载的 `references/`。
- 固定格式、计算或批处理优先用模板或脚本保证一致性。
- 至少通过一个正常项目和一个异常项目样例验证。
- 涉及财务、法务、广告合规或对外承诺时设置专项质量门槛。
